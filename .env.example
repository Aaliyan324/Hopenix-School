# ===========================================
# Hopenix School — Environment Variables
# ===========================================
# Currently no environment variables are required.
#
# Admin credentials are stored locally in the
# browser's localStorage (hashed with PBKDF2).
# Events data is also stored in localStorage.
#
# If you add backend services later, add their
# environment variables here with the VITE_ prefix.
